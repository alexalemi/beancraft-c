# Find the minimum of A and B -> Out
# Decrement A and B in lockstep, counting iterations in Out. Whichever register
# reaches zero first stops us, and Out then holds that count = min(A, B).
# (A and B are consumed.)

clrOut: - Out loop self

loop: - A done
- B done
+ Out loop
